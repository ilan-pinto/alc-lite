# PyPy-compatible dependencies for alc-lite (MINIMAL WORKING SET)
# Tested with PyPy 3.10 for optimal performance

# Core trading and market data
ib_async>=1.0.1
aeventkit==2.1.0

# Data processing (PyPy-optimized)
pandas>=1.5.0
numpy>=1.21.0

# Database connectivity (PyPy compatible)
# asyncpg>=0.27.0  # May have compilation issues with PyPy
# Note: psycopg2-binary is NOT compatible with PyPy
# For database access, consider using pure Python drivers or install manually

# Console output and formatting
rich==14.1.0
pyfiglet==1.0.4
markdown-it-py==4.0.0
mdurl==0.1.2
Pygments==2.19.2

# HTTP and web scraping
selenium==4.35.0
webdriver-manager==4.0.2
requests==2.32.5
urllib3==2.5.0
certifi==2025.8.3
charset-normalizer==3.4.3
idna==3.10
PySocks==1.7.1

# Utilities and async helpers
nest-asyncio==1.6.0
python-dateutil==2.9.0.post0
pytz==2025.2
tzdata==2025.2
six==1.17.0
typing_extensions==4.14.1
python-dotenv==1.1.1

# Development and testing (minimal set for PyPy)
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0
memory_profiler>=0.61.0

# Code formatting (PyPy compatible)
black>=22.0.0
isort>=5.10.0

# PyPy-specific optimizations
cffi==1.15.1
attrs==25.3.0
packaging==25.0

# Async and concurrency support
trio==0.30.0
trio-websocket==0.12.2
sniffio==1.3.1
outcome==1.3.0.post0
sortedcontainers==2.4.0
exceptiongroup==1.3.0
h11==0.16.0
wsproto==1.2.0
websocket-client==1.8.0

# System-level dependencies
greenlet==0.4.13
# Note: readline and hpy are built-in with PyPy - don't install via pip

# Notes for PyPy compatibility:
# 1. This is a MINIMAL working set - tested and verified
# 2. Excluded packages: psycopg2-binary, matplotlib, Pillow, openai
# 3. These can be added individually if needed but may require compilation
# 4. For visualization, use CPython environment instead
